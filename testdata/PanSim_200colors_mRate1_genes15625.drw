# This parameter file has been generated by the ALF web service.
# To run the simulation on your own machine, download ALF from http://www.cbrg.ethz.ch/alf
# and call alfsim from the parent directory of this file.

webRequest := false;
uuid := 'PanSim_colors200_mRate1_genes15625';

# name of simulation - you may want to change this
mname := uuid;

# directories for file storage - you may want to change these
wdir := '~/'.mname;
dbdir := 'Sequences/';
dbAncdir := 'IntermediateSequences/';

# time scale for simulation (PAM is default)
unitIsPam := true:

# parameters concerning the root genome
realseed := false;
protStart := 15625; # corresponds to individual genome lengths of approximately 5 Mb
minGeneLength := 100;
gammaLengthDist := [2.4019, 133.8063];
blocksize := 1:

# parameters concerning the species tree
treeType := 'BDTree';
birthRate := 0.01;
deathRate := 0.001;
NSpecies := 200; #number of colors in the graph
ultrametric := false;
mutRate := 1; #mutation rate
scaleTree := true;

# parameters concerning the substitution models
substModels := [SubstitutionModel('HKY',[0.66,0.33],[seq(0.25,4)], true)];
indelModels := [IndelModel(0.0003, ZIPF, [1.821], 50)];
rateVarModels := [RateVarModel(Gamma, 5, 0.01, 1)];
modelAssignments := [1]:
modelSwitchS := [[1]]:
modelSwitchD := [[1]]:

# parameters concerning genome rearrangement
invers := 0.025;
invSize := 5;
transloc := 0.025;
transSize := 5;
invtrans := 0.1;
fissionRate := 0;
fusionRate := 0;


# parameters concerning LGT
lgtRate := 0.0005;
orthRep := 0;
lgtGRate := 0.00005;
lgtGSize := 10;

# parameters concerning gene duplication
geneDuplRate := 0.001;
numberDupl := 5;
transDupl := 0.5;
fissionDupl := 0;
fusionDupl := 0;
P_pseudogene := 0;
P_neofunc := 0;
P_subfunc := 0;

# parameters concerning gene loss
geneLossRate := 0.001;
numberLoss := 5;

# output specification
simOutput := {'Fasta'};
